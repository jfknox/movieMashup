<!DOCTYPE html>
<html>
<head>
  <title>MovieMashup</title>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
  <link href='http://fonts.googleapis.com/css?family=Metal+Mania' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="navbar dark navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to image_tag("MashUp_logo.png", class: "nav-logo"), root_path %> </li>
        <li><%= link_to "Stories", stories_path, class: "nav-links" %> </li>
        <li><%= link_to "Posters", posters_path, class: "nav-links" %> </li>
      </ul>    
      <div class="nav navbar-nav navbar-right">
          <ul class="nav navbar-nav" id="user_avatar">
            <% if current_user %>
              <div class="dropdow-right">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><strong>
                   <%= image_tag current_user.info["info"]["image"], class: "prof_pic" %>

                    <%= current_user.name %></strong><span class="caret"></span>

                    ( <%=  current_user.reputation_for(:posters).to_i %> )
                    </a>

                    
                  <ul class="dropdown-menu" role="menu">
                    <li><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
                    <li><%= link_to "Profile", user_path(current_user) %></li>
                  </ul>
              </div>
            <% else %>
               <ul class="nav navbar-nav navbar-right">
                 <li><%=  link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %></li>
                </ul>
           <% end %>
          </ul>
      </div>
      <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
  </div>

</nav>


<%= yield %>



</body>
</html>
